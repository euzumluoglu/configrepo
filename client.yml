
server:
  port: ${PORT:8090}    
message: Hello world!!!!!!!!

#DOMAIN_NAME: "emn-cloud-client.herokuapp.com"
#heroku config:set DOMAIN_NAME="emn-cloud-client.herokuapp.com" --app emn-cloud-client
eureka:
  instance:
    hostname: ${DOMAIN_NAME}
    preferIpAddress: false 
    

