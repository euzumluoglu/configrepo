
eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://localhost:8761}/eureka/

security:
  oauth2:
    client:
      access-token-uri: http://localhost:8760/oauth/token
      user-authorization-uri: http://localhost:8760/oauth/authorize